# Redis configuration (shared)
# Used by Authelia, Nextcloud, Outline

# Disable persistence (cache-only)
save ""

# Memory limit
maxmemory 256mb
maxmemory-policy allkeys-lru

# Network
bind 0.0.0.0
protected-mode no
